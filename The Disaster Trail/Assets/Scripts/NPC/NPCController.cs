using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class NPCController : MonoBehaviour
{
    // Position offset for the billboarded image, adjust to your own liking
    public Vector3 DialogueBubbleOffset;

    // Position offset for the billoarded interact text, adjust to your own liking
    public Vector3 InteractTextOffset;
}
